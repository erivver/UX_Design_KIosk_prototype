# Kiosk Card Key Reissue Prototype

세로형 키오스크 카드키 재발급 프로토타입입니다.

## Local Preview

```bash
/Users/kanglee/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node server.js
```

브라우저에서 `http://localhost:5173`로 확인합니다.

## Static Deploy

이 프로토타입은 정적 파일로도 동작합니다. GitHub Pages, Vercel, Netlify에 루트 폴더 그대로 배포할 수 있습니다.

필수 파일:

- `index.html`
- `src/app.js`
- `src/styles.css`
- `assets/*`
